# utilhub
UtilHub is a terminal launcher that scans a utilities folder for single-file .NET tools, shows metadata and previews using Spectre.Console, and lets you build, run, or open utilities interactively with live output and spinner UI.
